import 'package:flutter/material.dart';
import 'package:sector_loyalty/theme.dart' as Theme;

class SideMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    Expanded userHeader = new Expanded(
        child: null;
    );

    final Expanded menuChoices = Expanded(child: null);

    final Expanded menuFooter = Expanded(child: null);

    return new Column(
      children: <Widget>[
        new Flex(
        ),
        new Flex(),
        new Flex(),
        ],
    );
  }
}
