
class RewardsCard {

  String cardId;
  String userId;
  String city;
  String state;
  String bgImage;
  String description;
  int currentPoints;
  int totalPoints;
  int currentMoney;
  int level;
  int nextLevel;
  int neededPoints;

  RewardsCard(this.cardId, this.userId, this.city, this.state, this.bgImage, this.description, this.currentPoints, this.totalPoints, this.currentMoney, this.level, this.nextLevel, this.neededPoints);


}